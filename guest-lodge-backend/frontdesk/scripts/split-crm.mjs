#!/usr/bin/env node

console.error('split:legacy is retired.');
console.error('simple-crm.html is now generated from the built frontdesk app and no longer contains source code.');
console.error('Edit frontdesk/src directly and run `npm run build:frontdesk`.');
process.exit(1);
