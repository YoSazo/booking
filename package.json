{
  "name": "marketel-booking-workspace",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev:local": "node scripts/dev-local.mjs",
    "verify": "npm --prefix guest-lodge-backend test && npm --prefix hotel-booking-app run lint && npm --prefix hotel-booking-app test && npm --prefix hotel-booking-app run build && npm --prefix marketel-frontdesk-ios run verify:release"
  }
}
